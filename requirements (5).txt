streamlit
tensorflow
pillow
numpy
requests
deep-translator
ddgs
anthropic

# Optional — enables a true continuous live-video feed on the Live Camera
# page (falls back to single-shot webcam capture if not installed)
streamlit-webrtc
av
opencv-python-headless
